# Backlog

- Consider adding a browser binary cache or alternate Playwright browser mirror for environments where apt/CDN access is blocked by proxy.
- Refresh or remove stale UI static assertions in `tests/ui_static/test_index_allowed_weekdays.mjs` so the full UI static suite can pass again.
